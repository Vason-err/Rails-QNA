<% unless @subscription.persisted? %>
$(".subscription").html('You are subscribed to this question')
$(".subscription").html('<%= link_to "Unsubscribe", question_subscription_path(question_id: @subscription.question_id), method: :delete, remote: true %>')
<% end %>